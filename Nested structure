#include <stdio.h>
struct Address {
    char city[50];
    int pincode;
};
struct Student {
    int id;
    char name[50];
    struct Address addr;  
};
int main() {
    struct Student s1;
    s1.id = 102;
    sprintf(s1.name, "Sri");
    sprintf(s1.addr.city, "Rajahmundry");
    s1.addr.pincode = 533101;
   Printf(“25331A05I2\n”);
    printf("ID: %d\n", s1.id);
    printf("Name: %s\n", s1.name);
    printf("City: %s\n", s1.addr.city);
    printf("Pincode: %d\n", s1.addr.pincode);
    return 0;
}
